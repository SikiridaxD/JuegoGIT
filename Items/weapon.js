class Weapon {
  name;
  minDmg;
  maxDmg;
  critical;

  constructor(name, minDmg, maxDamage, critical) {
    this.name = name;
    this.minDmg = minDmg;
    this.maxDmg = maxDamage;
    this.critical = critical;
  }
}
